/* assets/js/theme-toggle.js */
document.addEventListener('DOMContentLoaded', function () {
    const btn = document.getElementById('theme-toggle-btn');
    const menu = document.getElementById('theme-menu');

    if (!btn || !menu) return;

    // Logic repeated from inline script if we decide to move it here
    // For now, the inline script in the include handles it self-contained
    // keeping this file as a placeholder for more complex logic if needed
});
